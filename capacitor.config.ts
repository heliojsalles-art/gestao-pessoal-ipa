import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.helio.gestaopessoal',
  appName: 'Gestão Pessoal',
  webDir: 'dist',
};

export default config;
